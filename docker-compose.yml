version: '3.8'

networks:
  dev:

services:
  nginx:
    image: nginx:stable-latest
    ports:
      - '8080:80'
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
      - frontend

  backend:
    build: ./server
    ports:
      - '3000:3000'
    volumes:
      - ./server:/app
    environment:
      - NODE_ENV=development
    command: npm start
    networks:
      - dev

  frontend:
    build: ./webrtc-vue
    ports:
      - '8080:80'
    networks:
      - dev
